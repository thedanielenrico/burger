<h2>
  Burgers to Eat
</h2>
<ul>
  {{#each burgers}}
  {{#unless devoured}}
  <p>
    {{id}} {{burger_name}}
  </p>
  <button class="change-devour" data-id="{{id}}" data-devour="{{devoured}}">
    Eat it!
  </button>
  {{/unless}}
  {{/each}}
</ul>
<h2>
  Burgers that have been eaten!
</h2>
<ul>
  {{#each burgers}}
  {{#if devoured}}
  <p>
    {{id}} {{burger_name}}
  </p>
  {{/if}}
  {{/each}}
</ul>



<form>
  New burger: <input type="text" name="burgerName" id="newBurgerName" placeholder="name"><br>
  <input type="submit" value="Make it!" id="submit">
</form>